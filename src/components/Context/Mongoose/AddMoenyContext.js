import { createContext } from "react";

export const AddMoneyContext = createContext();

export const AddMoneyContextProvider = () => {

 return(
  <AddMoneyContext.Provider>
   
  </AddMoneyContext.Provider>
 )
}